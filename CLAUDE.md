# Transcriptum — parte do ecossistema Infinitum

> Este repositório é uma **casa** do ecossistema Infinitum. A constituição (regras, fatos, onde cada coisa vive) está em `../CLAUDE.md` (`Documents/Projects/Infinitum/CLAUDE.md`); leia-a primeiro — o Claude Code a carrega automaticamente por estar no diretório pai. Nada aqui a contradiz; se contradizer, a do pai vence.

**Antes de agir neste repo:** `whoami` → `get_context('transcriptum')` no MCP `infinitum-core`. É lá que estão estágio, próximo passo, tasks abertas, decisões vigentes e o que a última sessão fez — não em arquivos deste repo. Ao fechar: `record_session` com `project_slug='transcriptum'`.

Estágio rubedo: CLI de transcrição usado em produção (calls de cliente), com diarização. Mudanças aqui exigem os 60 testes passando antes de qualquer commit.
